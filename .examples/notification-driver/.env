# notification-driver example .env
#
# NOTIFICATION_DRIVER picks which module gets wired at bootstrap -- "email"
# or "sms". Comment it out (or delete the line) to prove Enum+Default falls
# back to "email" when the var is absent.
NOTIFICATION_DRIVER=sms
