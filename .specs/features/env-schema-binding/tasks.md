# Env → Schema Binding — Tasks

**This file is a stub.** `env-schema-binding` is planned together with its prerequisite
`dotenv-loading` (both are Milestone 19 "Config Loading", Track A = dotenv-loading, Track B =
env-schema-binding) in a SINGLE `tasks.md`, same pattern `graphql-realtime-protocols/tasks.md`
used to cover multiple protocol tracks in one file.

**Canonical source (single source of truth): `.specs/features/dotenv-loading/tasks.md`**

This feature's tasks are T9, T10, T11 (Track B) there, plus the shared T12 gate-final task. Do
not duplicate content here — if this stub and the canonical file ever appear to disagree, the
canonical file wins; update this stub to match instead of editing it independently.
